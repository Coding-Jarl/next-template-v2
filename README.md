Template for ValhallaCode's pet projects.

## Installation

- Clone project
- `npm ci`
- `npx husky install`
- `commitizen init cz-conventional-changelog --save-dev --save-exact`

Git commands

- `git add`
- `git cz`
- `git push`
